# Решение тестовых задач

Задача 1: Проверка email-доменов
Цель задачи
Разработать утилиту для массовой проверки MX-записей доменов email-адресов с целью определения их валидности для email-рассылок.

Функциональные требования
Входные данные:
Список email-адресов (максимум: 1000 адресов за один запуск)

Поддерживаемые форматы ввода:

Аргументы командной строки

Файл .txt (по одному email на строку)

Прямой ввод через stdin

Проверяемые критерии:
Синтаксис email - корректность формата (наличие @, допустимые символы)

Существование домена - проверка DNS A-записи

Наличие MX-записей - проверка DNS MX-записей

Возвращаемые статусы:
✅ «Домен валиден» - MX-записи найдены и корректны

❌ «Домен отсутствует» - домен не разрешается в DNS

⚠️ «MX-записи отсутствуют или некорректны» - домен существует, но нет MX-записей

Производительность:
Обработка 100 email-адресов: < 30 секунд

Параллельная проверка: до 10 одновременных DNS-запросов

Кэширование результатов: на время работы скрипта

Обработка ошибок:
Таймаут DNS-запросов: 5 секунд

Повторные попытки при сбоях: 2 раза

Graceful degradation при проблемах с сетью

Инструкция по запуску
Установка зависимостей:
bash
# Базовые зависимости
pip install dnspython

# Или через requirements.txt
pip install -r requirements.txt

Способ 1: Проверка одиночных email-адресов
bash
python email_validator.py user@example.com another@gmail.com
Способ 2: Проверка из файла
bash
# Создайте файл emails.txt
echo "test1@gmail.com" > emails.txt
echo "test2@company.com" >> emails.txt

# Запустите проверку
python email_validator.py --file emails.txt

# Или с указанием кодировки
python email_validator.py --file emails.txt --encoding utf-8
Способ 3: Пайплайн через stdin
bash
# Из другого скрипта или команды
cat email_list.txt | python email_validator.py --stdin

# Из генератора
python generate_emails.py | python email_validator.py --stdin

Опции командной строки:
bash
--file FILE          # Файл с email-адресами
--stdin              # Чтение из стандартного ввода
--output FORMAT      # Формат вывода: text, json, csv
--timeout SECONDS    # Таймаут DNS-запросов (по умолчанию: 5)
--parallel N         # Количество параллельных запросов (по умолчанию: 10)
--verbose            # Подробный вывод
--help               # Справка

 Задача 2: Мини-интеграция с Telegram
Цель задачи
Создать инструмент для автоматической отправки текстовых уведомлений из файлов в приватные Telegram-чаты через бота.

Функциональные требования
Входные данные:
Текстовый файл .txt любой кодировки

Максимальный размер файла: 10 MB

Поддержка длинных текстов (автоматическое разбиение)

Требования к Telegram боту:
Бот должен быть создан через @BotFather

Бот должен быть добавлен в приватный чат

Бот должен иметь права на отправку сообщений

Функционал отправки:
Поддержка markdown/HTML форматирования (опционально)

Автоматическое разбиение сообщений > 4096 символов

Отслеживание статуса отправки

Повторные попытки при сбоях

Инструкция по запуску
Способ 1: Python скрипт
Установка зависимостей:

bash
pip install python-telegram-bot requests
Базовое использование:

bash
# Отправка файла с указанием токена и chat_id
python telegram_sender.py \
  --token "1234567890:ABCdefGhIJKlmNoPQRsTUVwxyZ" \
  --chat "-1001234567890" \
  --file "notification.txt"

# С использованием конфигурационного файла
python telegram_sender.py --config config.yaml --file "message.txt"
С конфигурацией через environment variables:

bash
export TELEGRAM_BOT_TOKEN="your_token"
export TELEGRAM_CHAT_ID="your_chat_id"

python telegram_sender.py --file "alert.txt"
Дополнительные опции:

bash
--format html          # Форматирование как HTML
--format markdown      # Форматирование как Markdown
--parse-mode none      # Без форматирования
--silent              # Тихая отправка (без звука)
--disable-web-page-preview  # Без превью ссылок
--retry 3             # Количество повторных попыток
--delay 2             # Задержка между попытками (секунды)

🚀 Быстрый старт
Для задачи 1:
bash
# 1. Клонируйте или создайте проект
mkdir polza-email-checker && cd polza-email-checker

# 2. Установите зависимости
pip install dnspython

# 3. Создайте тестовый файл
echo -e "test@gmail.com\nexample@domain.com\ninvalid@fake12345.ru" > test_emails.txt

# 4. Запустите проверку
python email_validator.py --file test_emails.txt
Для задачи 2:
bash
# 1. Подготовьте рабочую директорию
mkdir telegram-notifier && cd telegram-notifier

# 2. Установите зависимости
pip install python-telegram-bot

# 3. Создайте бота и получите токен (через @BotFather)

# 4. Создайте тестовое сообщение
echo "Тестовое уведомление от Polza Outreach" > test_message.txt

# 5. Запустите отправку
python telegram_sender.py \
  --token "ВАШ_ТОКЕН" \
  --chat "ВАШ_CHAT_ID" \
  --file test_message.txt
